#ifndef CONTOUR_DETECTION_H
#define CONTOUR_DETECTION_H
#include <matrix.hpp>
#include <printing.hpp>
#include <points.hpp>

namespace ohe_framework{


unsigned int findContours(ImageMatrixUchar& image,std::vector<Blob>& contours);

bool plotContoursBoundary(ImageMatrixUchar& image, Blob &contours,
                  unsigned char red=255, unsigned char green=255,
                  unsigned char blue=255);
bool plotContoursFill(ImageMatrixUchar& image, Blob &contours,
                  unsigned char red=255, unsigned char green=255,
                  unsigned char blue=255);
class Line
{
public:
    Point start;
    Point end;
    Line();
    ~Line();
    bool drawLine(ImageMatrixUchar& image,
                  unsigned char red=255, unsigned char green=255,
                  unsigned char blue=255);
};
}// end namespace ohe_framework

#endif
